#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
// Bir ile 50 arasýndaki 5'e tam bölünebilen tam sayýlarýn c dilinde programlamasý.
int main(int argc, char *argv[]) {
	
/*	int i;
	for (i=1;i<=50;i++)
	{	
		if (i%5==0)
		
	{
			printf("%d\n",i);
}
	}
	
	 Bir ile 50 arasýndaki 5'e tam bölünebilen tam sayýlarýn c dilinde programlamasý.*/
	 
	/* int i;
	 for (i=1;i<50;i++)
	 {
	 	if(i%3==0)
	 	{printf("%d\n",i);  Bir ile 50 arasýndaki 5'e tam bölünebilen tam sayýlarýn c dilinde programlamasý.
		 }
	 }
	 int i;
	 for(i=1;i<50;i++)
	 {
	 	if(i%6==0)
	 	{
	 		printf("%d\n",i);
		 }
	 }
Bir ile 50 arasýndaki 5'e tam bölünebilen tam sayýlarýn c dilinde programlamasý.*/
	 
	int i;
	for(i=1;i<=30;i++)
	{
		if(i%2==0)
		{
		printf("%d\n",i);
		}
	 } 
	 
	 
	 
	 
	 
	 
	return 0;   
}

